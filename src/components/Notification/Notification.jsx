import css from "./Notification.module.css";


const Notification = () => (
  <div>
    <p className={css.textNotification}>No feedback has been submitted yet.</p>
  </div>
);

export default Notification;